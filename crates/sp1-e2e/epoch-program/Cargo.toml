[package]
name = "eudr-epoch-program"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
eudr-policy = { path = "../policy" }
sha2.workspace = true
sp1-zkvm = { version = "6.4.0", features = ["verify"] }
